#include <stdio.h>

/**
 * main - Program entry point
 * Print digit numbers of base 10 starting from 0
 * Return: Return 0 to stop the program
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
